# The deployment section should be excluded until
# it is explicitly included later on

#deployment:
#  dockerhub:
#    branch: master
#    commands:
#      - $DOCKER_HUB_TRIGGER

test:
  override:    
    - mocha
